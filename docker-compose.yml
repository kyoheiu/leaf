version: "3"
services:
  server:
    image: docker.io/kyoheiudev/acidpaper-server:v0.1.1
    container_name: server
    volumes:
      - ./server/databases:/home/server/databases
    ports:
      - 8000:8000
  client:
    build: client
    container_name: client
    ports:
      - 3000:3000
